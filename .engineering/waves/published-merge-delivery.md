# Published merge ancestry delivery repair

The operator-approved EKR completion authorizes this necessary dependency repair.
N=1, story:published-merge-delivery. Existing general shared-gates story is not
dispatched. The complete scheduling response is published-merge-selection.json.
Opening task records the reproduced refusal and exact required proof. The
planning CLI refused task scope (scope belongs to stories); the new story owns
the scope and the original task retains both its existing and new derivation.

Managed tree/id: ekr-gates-published-ancestry-20260922.
Branch: codex/ekr-gates-published-ancestry-20260922.
Published base: b829694b3f52ca319309c8b2b236467afa7ab421.
Dedicated build: <cache>/b10x-target/ekr-gates-published-ancestry-20260922.
Scratch: <cache>/ekr-completion-20260922/gates-ancestry.
Two compiler jobs; measure available storage and pause below the completion
plan's floor before large builds. No unavailable model-cost counters claimed.

Root owns AEP, normative docs, integration, publication and eventual release.
Implementor owns scoped Rust source/tests, writes no commits and acquires its own
lease. Independent adversary follows stable handback, then coordinator mutation
and full gate. This host lacks plugin subagent_type; explicitly load role
charters. Separate active units have separate managed trees and build output.
No EKR source or policy changes are part of this unit.

Implement the necessary generic verification behavior; Atlas stays neither
a dependency nor an installation requirement. The coordinator's earlier public-source
description of that reference checkout was incorrect: it is private. No private
paths, configuration, policy or credentials enter this public change. Preserve strict direct delivery,
unchanged adoption baselines, all scanners, signed receipts, coordinated hooks
and their previous hook chain. No wildcard committer exception or trusted local
remote-ref exemption. The runtime acceptance remains unexecuted at opening.

The implementor's retained report is published-merge-implementation.md. The
coordinator checked the latest published release through the bot API before
preparing the next patch version in Cargo.toml, Cargo.lock and CHANGELOG.md.
Those release files are coordinator-owned additions to the implementation scope;
the independent review and final gate run against that versioned candidate.
Release status remains pending until required source checks and published binary
and checksum verification complete.
